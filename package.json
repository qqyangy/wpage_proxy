{
  "name": "wpage_proxy",
  "version": "1.0.0",
  "description": "开发调试代理",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node main.js",
    "weinre-i": "npm install weinre -g",
    "weinre": "weinre --boundHost -all- --httpPort 8231",
    "i": "npm install && npm run weinre-i && sh ./bin/install.sh"
  },
  "author": "yong.yang",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "uglify-js": "^3.16.1"
  }
}